server {
    listen 8080;
    server_name _;

    root /app/site;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~* \.json$ {
        default_type application/json;
    }
}
